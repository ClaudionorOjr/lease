# TODO Criar variáveis de ambiente para postgresql
services:
  pg:
    image: bitnami/postgresql:latest
    container_name: scheduling-database
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=scheduling